import { StoryLinkDirective } from './story-link.directive';

describe('StoryLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new StoryLinkDirective();
    expect(directive).toBeTruthy();
  });
});
